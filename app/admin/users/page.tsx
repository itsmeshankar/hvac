import { AdminCrudPage } from "@/components/admin/admin-crud-page";
export default function UsersAdminPage() { return <AdminCrudPage title="Admin Users" endpoint="/api/admin/users" note="Only Super Admin can create, update, or delete users. Password is required when creating a user and optional when updating." fields={[{ name: "name", label: "Name", required: true }, { name: "email", label: "Email", required: true }, { name: "password", label: "Password", type: "password" }, { name: "role", label: "Role", type: "select", options: ["admin", "superadmin"] }, { name: "status", label: "Status", type: "select", options: ["active", "inactive"] }]} />; }
